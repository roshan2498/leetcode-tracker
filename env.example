# Database Configuration
# For Vercel Postgres: Get from Vercel Dashboard -> Storage -> Your Database -> .env.local tab
DATABASE_URL="postgresql://username:password@host:port/database?sslmode=require"

# NextAuth.js Configuration
# Generate a random secret: openssl rand -base64 32
NEXTAUTH_SECRET="your-super-secret-key-here"

# For production deployment, set this to your domain
NEXTAUTH_URL="https://your-app-name.vercel.app"

# Google OAuth Configuration (Required for authentication)
# Get these from: https://console.developers.google.com/
GOOGLE_CLIENT_ID="your-google-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# Optional: Additional OAuth Providers
# GitHub OAuth (Optional)
GITHUB_ID="your-github-oauth-app-id"
GITHUB_SECRET="your-github-oauth-secret"

# Vercel Configuration (For GitHub Actions)
# Get these from: https://vercel.com/account/tokens
VERCEL_TOKEN="your-vercel-token"
VERCEL_ORG_ID="your-vercel-org-id"
VERCEL_PROJECT_ID="your-vercel-project-id"

# Development Configuration
NODE_ENV="production"

# Optional: Analytics & Monitoring
# Vercel Analytics (Optional)
NEXT_PUBLIC_VERCEL_ANALYTICS_ID="your-analytics-id"

# Optional: Error Tracking
# Sentry (Optional)
SENTRY_DSN="your-sentry-dsn"
SENTRY_AUTH_TOKEN="your-sentry-auth-token" 